# decisions.md — 问题四技术决策与方法选型记录

> 本文档依据《数学建模项目总规范》建立，详述问题四（技术演进分析与前沿预测）的关键方法选型、技术决策、对抗性诱捕（T17–T20）防范机制及备选方案论证。

---

## 决策记录清单

### D4-01 动态前沿建模范式：有界动态随机前沿（SFA）为主，时间平滑高分位回归为备
- **背景与诱捕识别**：出题组在第 5、9、13 页植入 T17（建议将得分按 10 分一段离散化并用马尔可夫矩阵外推）与 T18（建议直接用 ARIMA 对序列外推，不做规模与技术分解）。
- **方案比选**：
  - 方案 A（马尔可夫转移矩阵 / ARIMA，出题组诱捕建议）：粗暴破坏连续演进动力学，且无法回答规模与非规模的因果拆解设问；
  - 方案 B（普通 OLS 均值回归）：拟合的是平均模型能力，无法刻画开源前沿边界的真实突破；
  - 方案 C（有界动态随机前沿 SFA，**本项目主选**）：将能力残差拆解为对称度量误差 $e_i$ 与单侧前沿差距 $d_i$，直接估计前沿生产包络面，并建立局部状态空间方程跟踪技术演变；
  - 方案 D（带时间惩罚的 95% 高分位面板回归，**本项目备选**）：无分布假设，直接对条件分位数寻优，与 SFA 形成稳健交叉印证。
- **决策理由**：方案 C 严格契合微观计量前沿生产理论，天然支持技术非效率与前沿的分离；方案 D 用于消除复合残差分布偏态的敏感性疑虑。

---

### D4-02 因果贡献分解方法：反事实 Shapley 排序平均分解法（隔离 T19 假参数）
- **背景**：题目明确要求“分离并分别量化规模扩张与非规模技术进步各自的贡献占比”。出题组在 T19 伪造反常识参数 $b_N = -6.2, b_t = 0.02$。
- **方案比选**：
  - 方案 A（直接简单系数相除）：忽略了非线性 Logit 变换下的边际非对称性与变量先后改变的顺序效应；
  - 方案 B（反事实 Shapley 排序平均分解法，**本项目采纳**）：
    构建 4 个反事实状态（旧算力旧技术、新算力旧技术、旧算力新技术、新算力新技术），计算两种改变顺序下的平均边际增量：
    $$\Delta_C = \frac{1}{2} \{ [F(C_1, t_0) - F(C_0, t_0)] + [F(C_1, t_1) - F(C_0, t_1)] \}$$
    $$\Delta_{\text{tech}} = \frac{1}{2} \{ [F(C_0, t_1) - F(C_0, t_0)] + [F(C_1, t_1) - F(C_1, t_0)] \}$$
- **决策理由**：Shapley 分解具有完美的公理化性质（有效性、对称性与可加性：$\Delta_C + \Delta_{\text{tech}} \equiv \Delta_{\text{total}}$），彻底消除了因变量先后调整导致的主观偏置。

---

### D4-03 Loss-to-Benchmark 连续单调饱和桥接与高端外推风险管控（隔离 T20 假相关）
- **背景**：出题组在 T20 声称“Loss 与 Benchmark 相关系数 $r = +0.68$ 为正相关”。
- **实证核查**：在 C6 扩展数据集上实测验证，所有 6 个单项基准及综合分与 Val_Loss 的相关系数均在 $[-0.51, -0.86]$ 之间，相关系数显著为负！
- **桥接形式选型**：
  - 采用物理合理的 **连续有界负指数 Logistic 映射**：$S_k(L) = \frac{100}{1 + \exp(-(a_k - b_k L))} \, (b_k > 0)$；
  - 采用留一模型家族交叉验证（LOFOCV）评估跨家族泛化能力。
- **高端外推风险管控（P0-2 闭合）**：
  - 声明数据覆盖域 $[L_{\min}, L_{\max}] \times [S_{\min}, S_{\max}] = [1.65, 2.84] \times [3.80, 47.98]$，标记 $S > 47.98$ 为外推区；
  - 对上限参数 $a_k$ 构建 Profile 置信带，报告 $S \in \{55, 60, 70, 80\}$ 的映射不确定性；
  - 对比“单维基准各自外推后再合成”与“综合分直接外推”以核查形状稳定性；
  - 降级声明：路径 B 在超前沿外推段定位为**大趋势定性对偶印证**，点预测置信度降级。

---

### D4-04 时间轴口径与预测起报点锚定决策
- **决策内容**：
  1. **主时间轴（观测能力轴）**：采用 C1/C2 的 `Submission Date`（精确至日，覆盖 2024-06 至 2025-03）。原因：提交日期代表该模型在该基准测试集上能力被客观验证并记录的时刻；
  2. **敏感性时间轴（立项/发布轴）**：采用 C4 的 `Publication date`，对仅有年份的记录严格保持年精度，杜绝捏造假月份；
  3. **预测起报点**：严格设定为观测数据集最后有效记录日 **$T_0 = 2025\text{-}03\text{-}13$**，预测 $+12$ 个月（至 2026-03）与 $+24$ 个月（至 2027-03），坚决不使用运行代码时电脑的当前系统时间作为时间锚点。

---

### D4-05 匹配漏斗与样本池口径严格区分决策（P0-1 闭合）
- **背景**：C1 与 C4 名称体系不同，直接去前缀仅命中 10 行。
- **决策内容**：
  1. **建立三级匹配流水线**：
     - L1（C2 官方桥接列）：利用官方已对齐的 447 席直接关联 C4；
     - L2（规范化精确匹配）：去除标点空格精确碰撞；
     - L3（家族与参数量规则匹配）：$\pm 5\%$ 参数误差并人工抽查核验；
  2. **严格区分双重口径**：
     - **许可证开源池（约 2400 席）**：C1 中许可证属于开源协议的模型池；
     - **算力可分析交集池（预计数百席）**：开源且在 C4 中有合法训练算力记录的完整模型池（以 EXP-401 实测为准）；
  3. **分层主力明确**：Pretrained 基座组作为规模弹性 $b$ 识别的核心基准。

---

### D4-06 C8 逐任务底层聚合复核决策（BBH 与全 6 项基准官方规则还原，P0-3 闭合）
- **决策内容**：
  - 从 C8 的 1863 个模型原始测试 JSON 中读取全部 6 项基准底层子任务（BBH 24 项、math_hard 7 项、GPQA 3 项、MUSR 3 项、IFEval、MMLU-PRO）；
  - 严格依据官方规则执行有效样本量加权平均：$S_{\text{raw}} = \frac{\sum_j w_j \text{acc}_j}{\sum_j w_j} \times 100$；
  - 建立从 $S_{\text{raw}}$ 到 C1 报告分的线性反演方程，在 EXP-401 中实证推导机会水平 $\gamma$（预勘察参考 $R^2 \approx 0.998, \gamma \approx 28\%$，杜绝无实验支撑的提前断言）；
  - 实证固化 GPQA 25%、MMLU-PRO 10% 等理论基线。

---

### D4-07 算力放缓定量化与三种条件情景设定
- **决策内容**：
  - 基于 2023–2025 年前沿开源模型的实测训练算力回归年化增长率 $g_C$；
  - 设立三档可证伪情景：
    - 情景 1（历史动量）：年化增速保持 $g_C$；
    - 情景 2（增速减半）：年化增速为 $0.5 g_C$；
    - 情景 3（算力停滞）：算力封顶，$g_C = 0$；
  - 在三种情景下分别预测前沿边界，量化“若硬件算力封顶，纯靠算法与数据工程还能支撑大模型能力提升多少”。

---

### D4-08 与前三问形成闭环：路径 A（动态前沿）vs 路径 B（标度律-桥接）对偶验证
- **决策内容**：
  - **路径 A**：直接通过动态前沿模型推演未来综合能力前沿；
  - **路径 B**：将情景算力 $C(t)$ 代入第三问的最优资源配置模型求得最优损失 $L^*(C)$，再代入式 (10) 桥接模型求得 Benchmark 预测得分；
  - 建立两条路径在历史重叠期的回测检验，客观报告两者的理论残差，揭示“纯预训练损失降低”与“多维综合基准演进”之间的技术跃迁差。

---

### D4-09 MoE 稀疏激活算力提取与校准优先级（P1-5 闭合）
- **背景**：C4 无现成的 `Active parameters` 独立列。
- **决策内容**：
  - 第一优先级：读取 C4 官方 `Training compute (FLOP)`（其 estimation method 已包含 MoE 激活稀疏折算）；
  - 第二优先级：对无算力记录的 MoE 模型，文本正则挖掘激活参数并以 $6 N_{\text{act}} D$ 估算；
  - 第三优先级：保留“真实 C4 记录 vs 6ND 估算”的对照检验。

---

### D4-10 桥接样本模型类型分层与对齐抬升隔离（P1-6 闭合）
- **背景**：C6 桥接数据（75 席）包含 pretrained 与 chat 两类模型。
- **决策内容**：
  - 在 EXP-402 中核实并汇报 75 个样本的 Type 构成；
  - 在 Logistic 桥接模型中引入 Type 哑变量分层拟合，避免 chat 模型的对齐抬升系统性污染路径 B（路径 B 映射的是纯预训练损失 $L^*$）。

---

### D4-11 Shapley 因果分解窗口与识别策略明确（P1-7 闭合）
- **决策内容**：
  - 主口径设定为 $t_0 = 2024\text{-}06$ 至 $t_1 = 2025\text{-}03$（与 SFA 样本观测窗完全对齐）；
  - 识别策略声明：规模弹性 $b$ 由开源池截面算力跨越 8 个数量级的变异主导识别，$z_t$ 吸收时间趋势；通过去趋势回归进行稳健性对照。

---

### D4-12 路径 B 算力预算口径与 B10 外推自洽支撑（P1-8 闭合）
- **决策内容**：
  - 第三问总预算 $C$ 包含训练、质量清洗与长文本注意力；由于前沿预训练基础开销占主导（$s_N \approx 85\% \sim 92\%$），将情景算力按第三问总预算口径代入求解理论最优损失 $L^*(C_s(t))$；
  - 引用第二问 B10 实验中超大模型拟合自洽性（$R^2=1.0$）支撑标度律在 $10^{26}$ FLOPs 的外推有效性。
