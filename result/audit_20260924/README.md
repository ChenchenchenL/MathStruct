# 2026-09-24 交接事项回传

基准提交 `f9e61755f72b3f8b040d36000ea9f4ebbf1b9305`。本目录是新增审计，不覆盖 `result/problem01` 至 `result/problem04` 的原实验。旧运行日志没有找回，也没有补造。

环境：`D:\Program Files\CondaEnvs\AIOPS\python.exe`，Python 3.11.15，numpy 2.4.6，pandas 3.0.6，scipy 1.17.1，scikit-learn 1.9.1，matplotlib 3.11.2。

命令：

```text
python src/audit/audit_20260924_t01_t10.py
```

该命令重跑两次。第二次只修正持平基线的起点，结果以第二次为准。随机种子 `20260924` 只用于记录；前沿拟合本身沿用原求解器，没有新的随机步骤。九组预算抽查使用差分进化种子 42，与问题三原求解器一致。

## 文件

| 文件 | 内容 |
|---|---|
| `audit_summary.json` | T01、T02、T05、T06、T08 的汇总数字 |
| `t01_oot_monthly.csv` | 六个测试月的真实最高分、预测、区间和覆盖 |
| `t01_oot_baselines.csv` | 同一划分上的持平、训练期末水平和训练期趋势 |
| `t02_decline_decomposition.csv` | 12/24 个月预测拆成规模项与技术状态项 |
| `t02_fitted_technology_state.csv` | 全样本拟合的月度技术状态 |
| `t06_rank_by_predictor.csv` | 按预测器和数据性质列出的相关 |
| `t08_q_regime_from_saved_allocation.csv` | 已保存配置的质量边界 |
| `t08_qbase_spot_rerun_p0.csv` | 九组基准配比的定向复算 |
| `t04_version_status.json` | 问题一五个脚本的记录哈希与现状 |
| `file_hashes.json` | 审计开始时 `src/` 与 `result/` 的哈希 |
| `run.log` | 第二次运行的控制台记录 |

## 逐项状态

### T01 已补证，预测主张保留限制

六个测试月复算 MAE 为 12.86，95% 覆盖 4/6，80% 覆盖 2/6，与 `exp05_summary.json` 一致。同一对象上，持平预测 MAE 2.81，训练期末水平 MAE 2.23，训练期线性趋势 MAE 4.73。现有前沿模型比这三种基线差。2025-02 的绝对误差 20.73 分，对应最高分模型算力降到 `2.5e23` FLOPs，不是普遍的月份漂移。

这只支持“该划分上的月度最高分没有被当前模型可靠跟踪”。不支持 12 个月或 24 个月最高能力预测。敏感性表里另三种覆盖率原先是占位符，已从 `tab_p4_oot_validation_metrics.csv` 清空；`exp05_summary.json` 保留原值，避免改写旧实验记录。

### T02 已补证，定义未改

预测对象仍是 SFA 条件期望前沿，不是截至该月仍可选模型的最高分，也不是当月新模型的最高分。全样本复算起点 42.60，情景一 12 个月 35.91，24 个月 35.32，与原表一致。

分解：只推进算力、冻结技术状态时，12 个月前沿升至 49.42；只外推技术状态、冻结算力时，降至 29.85。回落来自技术状态外推，不来自规模项。拟合状态在 2025-03 为 −0.212，低于 2025-01 的 −0.030。外推斜率按代码写成两月差分除以 3，得到每月 −0.0605；按两月跨度除以 2 则为 −0.0908。代码没有改，因为改小分母会让下降更大，不能用来制造上升。

### T03 保留限制

问题一质量表仍被读取，但只记录七域加权值 0.4938，不进入配比响应或标度律。配比模型继续在 A4/A5 上重拟合。A 组质量分与 B 组 `Q` 没有建立等值换算。对应说明已写入 `exp03_mixture_transfer.py` 和 `load_p1_quality_table`。

### T04 保留缺失

问题一 EXP-01、EXP-03、EXP-04 的记录哈希与当前文件一致。EXP-02 记录哈希不在 git 历史中；当前文件哈希为 `34a68f17…`。EXP-05 记录哈希同样不在历史中；当前文件及 `exp05_summary.json` 内嵌哈希均为 `e82dfeac…`，二者一致，但与实验记录不一致。没有运行日志，故不声称结果由当前文件生成。问题二至问题四没有记录脚本哈希。`exp04_summary.json` 的区间字段仍不是 `exp04` 脚本写出的。

### T05 保留限制

路径 B 使用复合预算，不是纯训练算力约束。`0.9602` 仍是硬编码情景值。已保存的路径 B Loss 为 1.798–1.849，落在 C6 的 1.65–2.84 内。两路径分差仍是口径差，不称为技术红利。

### T06 已补证，失败保留

10B/70B 的 −0.5157/−0.5684 来自全样本 OLS，目标是估算损失表，各 63 行。树模型没有参与这组相关。1M 训练表上的树模型交叉验证 `R²` 为 0.714，对象不同。

### T07 保留限制

没有首次预测、查看标签和冻结模型的时间记录。A6/A7 继续按复评写。本次没有重训。

### T08 待决定，方向抽查未变

0.584 不能由冻结的域中位数表复现。Common Crawl 0.6498 与 C4 0.5096 的样本量加权均值为 0.5784。原始行的合并中位数未重算。已保存的 18 行配置中，低预算指数成本在内部点，低预算幂成本贴下界，其余贴上界。九组基准配比按原下界复算，边界类型没有变化。是否把 0.584 改为 0.578 仍是情景选择；这一差距小于下界本身的取整，不改变上述方向。

### T09 已纠注释，最优性限制保留

路径 B 的注释由“250 个起点”改为实际的 50 个网格起点加两条边界搜索。问题三文档仍写差分进化 `popsize=20`、`maxiter=2000`，代码是 `popsize=15`、`maxiter=500`。全局搜索在问题四关闭。没有补存 300 组参考配置；原压力结果只保留损失，不补造配置。

### T10 保留限制

桥接 n=75、`R²=0.4916`、RMSE 8.31、留模型族 MAE 8.16 未改。868 条由 109 条基座和 759 条对话构成。83.3% 是对话前沿模型内的 Shapley 份额，不是样本比例，也不是纯技术因果贡献。逐条许可证匹配明细没有新建。
